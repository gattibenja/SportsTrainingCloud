# Example environment variables for SportsTraining (SportsTraining - Copy)

# Backend (set these in Render / your backend host)
# MONGO_URI: MongoDB connection string
MONGO_URI='mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/DBNAME?retryWrites=true&w=majority'
# JWT secret used to sign tokens
JWT_SECRET='replace_with_a_strong_random_secret'
# Comma-separated list of allowed origins for CORS (include your Pages domain)
CORS_ALLOWED_ORIGINS='https://your-project.pages.dev,https://www.yourdomain.com,http://localhost:5173'
# Optional admin secret used in signup flow
CLAVE_ADMIN='your_admin_secret'
# Node env and port
NODE_ENV=production
PORT=4000


# Cloudflare Pages / Functions (set these in Pages project settings)
# BACKEND_URL should point to your public backend (Render) so Functions can proxy.
BACKEND_URL='https://your-backend.onrender.com'
# ALLOWED_ORIGINS: origins that Functions will accept and echo in ACAO
ALLOWED_ORIGINS='https://your-project.pages.dev,https://www.yourdomain.com'
# VITE_REACT_APP_API_URL: recommended to leave empty in Pages to use relative /api paths
# If you prefer, set to the Pages domain to force using the proxy
VITE_REACT_APP_API_URL=''

# Local development (examples)
# In local dev you may keep VITE_REACT_APP_API_URL=http://localhost:4000 to hit backend directly
# and use BACKEND_URL=http://localhost:4000 when running `wrangler pages dev --bindings BACKEND_URL=...`
